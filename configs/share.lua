-- Share sheet. "Nearby" share targets are players who currently have their
-- phone out (open) within `Range` metres of you.
-- 分享面板。“附近”分享目标是指在你 `Range` 米范围内、当前正把手机拿在手上（打开状态）的玩家。
return {
    Range      = 10.0,  -- metres 米
    MaxTargets = 12,    -- 最多可显示的附近目标数量
}
